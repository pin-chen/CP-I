#include <bits/stdc++.h>
#define ll long long
#define pii pair<int, int>
#define fst first
#define sec second
using namespace std;
int main(){
	//ios::sync_with_stdio(0);
	//cin.tie(0);
	int n;
	cin >> n;
	int W[n], H[n];
	for(auto &x : H) cin >> x;
	for(auto &x : W) cin >> x;
	
	return 0;
} 
